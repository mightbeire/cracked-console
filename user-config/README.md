# Local User Configuration

Keep your personal plan files in this directory.

Git ignores the contents of this directory.

Copy the public example:

```text
examples/starter-plan.example.json
```

to:

```text
user-config/plan.json
```

Then edit `user-config/plan.json`.

Do not put API keys, tokens, passwords, private keys, or other secrets in the plan file.
